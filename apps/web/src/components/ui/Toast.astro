---
// Toast container — toasts are injected dynamically via window.showToast() from toast.ts
---

<div
	id="toast-container"
	class="fixed top-4 right-4 z-80 flex flex-col gap-2 max-w-sm w-full pointer-events-none"
	aria-live="polite"
>
</div>
